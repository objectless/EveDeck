node_modules
.next
out
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.env
.env.local
.DS_Store

# Large release artifacts — served from GitHub Releases instead
site/public/files/*.zip
site/public/files/*.exe
publish/
sc-winx64/
settings.local.json
*.tsbuildinfo

# Local agent tooling and machine-specific credentials — never commit
.claude/
.frugal-fable/
.vscode/
.env.example
deploy.local.env
CLAUDE.local.md
CLAUDE.md
AGENTS.md
site/

# Stray working copies of the logo dropped in the repo root — the canonical
# app icon/logo assets live under app/src/EveDeck.
/ChatGPT Image *.png
